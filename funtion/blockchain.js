const wallet  = require('../controller/user/user');
//axios 추가해서 블록체인 서버랑 통신.

module.exports  = {
  test : async function(data){
    try {       
    } catch (err) {
      console.error(err);
    }
    return data;
  },//test
}